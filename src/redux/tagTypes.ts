export enum tagTypes {
  user = "user",
  serviceProvider = "service_provider",
  admin = "admin",
  superAdmin = "super_admin",
}

export const tagTypesList = [tagTypes.user, tagTypes.serviceProvider, tagTypes.admin];
